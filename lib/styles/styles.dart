import 'package:flutter/material.dart';

const cardMargin = EdgeInsets.symmetric(vertical: 10);
const cardPadding = EdgeInsets.all(12);
const cardBorderRadius = BorderRadius.all(Radius.circular(16));

const eventTitleStyle = TextStyle(fontSize: 18, fontWeight: FontWeight.bold);

const eventDateStyle = TextStyle(fontSize: 14, color: Colors.black87);

const iconColor = Colors.grey;
